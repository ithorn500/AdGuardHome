{
  "schema": "amber.bus.client.version.v1",
  "client_id": "adguardhome",
  "framework_version": "0.4.0",
  "client_version": "0.2.0",
  "framework_version_owner": "amber-bus",
  "version_owner": "adguardhome-fork",
  "reporting_fields": [
    "bus.client.framework_version",
    "bus.client.version"
  ],
  "notes": "framework_version tracks the Amber Bus consumer framework pack (src/amber_bus/generator.py CLIENT_FRAMEWORK_VERSION, 0.4.0 as served today) and is owned by amber-bus. client_version tracks only this fork's own connector code and is not the AdGuard Home version: the app is built on the edge channel and reports v1.0.0-a.N+<sha>. Raised 0.1.0 -> 0.2.0 on 2026-08-16 for the /control/status amber_bus block and the connector-state mark in client_v2. The bus manifest declares local_manifest_required and names this file; it did not exist until now, so nothing was reporting these values. Note that the connector still has no registration or heartbeat code, so this file is a declaration the bus cannot yet collect - see the audit for that gap.",
  "source_packages": [
    "internal/amberbusconnector/",
    "internal/home/amberbus.go"
  ]
}
